const Footer = ({className = 'footer', ...props}) => {
    return(
        <div
            className={className}
            {...props}
        >
            Footer test
        </div>
    )
}

export default Footer;